There are two tables created in the databse for this project i.e. user and words.

The first one is user table, which is used to store the username, password and email of the user for its authentication.

The second is words table. It contains four column i.e id, word, meaning and email. The email column stores the email of the user which will addd the words. 